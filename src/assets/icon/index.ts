import {ImageStyle, StyleProp} from 'react-native';
import {Icon, IconElement, IconSource} from './icon.component';

export const AcneIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/acne.png'),
  };

  return Icon(source, style);
};

export const ArmIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/arm.png'),
  };

  return Icon(source, style);
};
export const WeightIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/weight.png'),
  };

  return Icon(source, style);
};
export const HealthGoodIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/health-good.png'),
  };

  return Icon(source, style);
};
export const BleedingIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/bleeding.png'),
  };

  return Icon(source, style);
};

export const StomachAcheIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/stomach-ache.png'),
  };

  return Icon(source, style);
};

export const HeadAcheIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/headache.png'),
  };

  return Icon(source, style);
};

export const BackPainIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/back-pain.png'),
  };

  return Icon(source, style);
};
export const ChestPainIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/chest-pain.png'),
  };

  return Icon(source, style);
};
export const FaintingIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/fainting.png'),
  };

  return Icon(source, style);
};
export const HealthCareIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/healthcare.png'),
  };

  return Icon(source, style);
};

export const ImportantIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/reminder.png'),
  };

  return Icon(source, style);
};
export const ScheduleIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/schedule.png'),
  };

  return Icon(source, style);
};
export const CalendarIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/calendar.png'),
  };

  return Icon(source, style);
};

export const MilestoneIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/milestone.png'),
  };

  return Icon(source, style);
};
export const LocationPinIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/location-pin.png'),
  };

  return Icon(source, style);
};
export const PlusIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/plus.png'),
  };

  return Icon(source, style);
};
export const LeftIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/Left.png'),
  };

  return Icon(source, style);
};

export const RemoveIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/remove.png'),
  };

  return Icon(source, style);
};

export const NoDataIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/no-data.png'),
  };

  return Icon(source, style);
};

export const ChangeTimeIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/change-time.png'),
  };

  return Icon(source, style);
};
